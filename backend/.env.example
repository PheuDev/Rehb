DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/rehab
PORT=8000
CORS_ORIGIN=http://localhost:5173

# Authentification JWT
# Générer avec : python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=changez-cette-valeur-en-production-avec-une-cle-secrete-forte
ACCESS_TOKEN_EXPIRE_MINUTES=480

# Mot de passe du compte admin créé au premier démarrage (si aucun admin n'existe)
ADMIN_PASSWORD=admin
